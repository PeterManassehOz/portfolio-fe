export interface SiteInfo {
  name: string;
  firstName: string;
  lastName: string;
  title: string;
  shortBio: string;
  email: string;
  location: string;
  availability: string;
  resumeUrl: string;
  profileImage: string;
}